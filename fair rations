def fairRations(B):
    total_loaves = 0
    n = len(B)

    for i in range(n - 1):
        if B[i] % 2 != 0:  
            B[i] += 1
            B[i+1] += 1
            total_loaves += 2

    
    if B[-1] % 2 != 0:
        return "NO"
    else:
        return str(total_loaves)
